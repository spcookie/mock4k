{
  "basicTemplates": {
    "simpleUser": {
      "id": "{{int(1,1000)}}",
      "name": "{{name}}",
      "email": "{{email}}",
      "age": "{{int(18,80)}}"
    },
    "basicProduct": {
      "id": "{{uuid}}",
      "name": "{{product}}",
      "price": "{{float(1.0,999.99)}}",
      "inStock": "{{boolean}}"
    },
    "simpleAddress": {
      "street": "{{address}}",
      "city": "{{city}}",
      "zipCode": "{{zipcode}}",
      "country": "{{country}}"
    }
  },
  "complexTemplates": {
    "detailedUser": {
      "id": "{{int(1,10000)}}",
      "profile": {
        "firstName": "{{firstName}}",
        "lastName": "{{lastName}}",
        "email": "{{email}}",
        "phone": "{{phone}}",
        "birthDate": "{{date(1950-01-01,2005-12-31)}}",
        "avatar": "{{imageUrl}}"
      },
      "address": {
        "street": "{{address}}",
        "city": "{{city}}",
        "state": "{{state}}",
        "zipCode": "{{zipcode}}",
        "country": "{{country}}",
        "coordinates": {
          "latitude": "{{float(-90.0,90.0)}}",
          "longitude": "{{float(-180.0,180.0)}}"
        }
      },
      "preferences": {
        "language": "{{randomOf(en,zh,es,fr,de)}}",
        "timezone": "{{timezone}}",
        "notifications": {
          "email": "{{boolean}}",
          "sms": "{{boolean}}",
          "push": "{{boolean}}"
        }
      },
      "metadata": {
        "createdAt": "{{timestamp}}",
        "updatedAt": "{{timestamp}}",
        "version": "{{int(1,10)}}",
        "tags": "{{array(string,1,5)}}"
      }
    },
    "ecommerceOrder": {
      "orderId": "{{uuid}}",
      "customer": {
        "id": "{{int(1,10000)}}",
        "name": "{{name}}",
        "email": "{{email}}",
        "loyaltyLevel": "{{randomOf(bronze,silver,gold,platinum)}}"
      },
      "items": [
        {
          "productId": "{{uuid}}",
          "name": "{{product}}",
          "category": "{{randomOf(electronics,clothing,books,home,sports)}}",
          "price": "{{float(5.0,500.0)}}",
          "quantity": "{{int(1,10)}}",
          "discount": "{{float(0.0,0.5)}}",
          "specifications": {
            "weight": "{{float(0.1,50.0)}}",
            "dimensions": {
              "length": "{{float(1.0,100.0)}}",
              "width": "{{float(1.0,100.0)}}",
              "height": "{{float(1.0,100.0)}}"
            },
            "color": "{{color}}",
            "material": "{{randomOf(plastic,metal,wood,fabric,glass)}}"
          }
        }
      ],
      "shipping": {
        "address": {
          "street": "{{address}}",
          "city": "{{city}}",
          "zipCode": "{{zipcode}}",
          "country": "{{country}}"
        },
        "method": "{{randomOf(standard,express,overnight,pickup)}}",
        "cost": "{{float(0.0,50.0)}}",
        "estimatedDelivery": "{{date(+1d,+14d)}}"
      },
      "payment": {
        "method": "{{randomOf(credit_card,debit_card,paypal,bank_transfer)}}",
        "amount": "{{float(10.0,1000.0)}}",
        "currency": "{{randomOf(USD,EUR,GBP,JPY,CNY)}}",
        "status": "{{randomOf(pending,completed,failed,refunded)}}",
        "transactionId": "{{uuid}}"
      },
      "status": "{{randomOf(pending,processing,shipped,delivered,cancelled)}}",
      "timestamps": {
        "ordered": "{{timestamp}}",
        "processed": "{{timestamp(+1h)}}",
        "shipped": "{{timestamp(+1d)}}",
        "delivered": "{{timestamp(+3d)}}"
      }
    },
    "socialMediaPost": {
      "id": "{{uuid}}",
      "author": {
        "id": "{{int(1,1000000)}}",
        "username": "{{username}}",
        "displayName": "{{name}}",
        "avatar": "{{imageUrl}}",
        "verified": "{{boolean}}",
        "followers": "{{int(0,1000000)}}",
        "following": "{{int(0,10000)}}"
      },
      "content": {
        "text": "{{lorem(10,280)}}",
        "hashtags": "{{array(hashtag,0,10)}}",
        "mentions": "{{array(username,0,5)}}",
        "media": [
          {
            "type": "{{randomOf(image,video,gif)}}",
            "url": "{{imageUrl}}",
            "thumbnail": "{{imageUrl}}",
            "duration": "{{int(1,300)}}",
            "size": "{{int(1024,10485760)}}"
          }
        ]
      },
      "engagement": {
        "likes": "{{int(0,100000)}}",
        "shares": "{{int(0,10000)}}",
        "comments": "{{int(0,5000)}}",
        "views": "{{int(0,1000000)}}"
      },
      "metadata": {
        "createdAt": "{{timestamp}}",
        "updatedAt": "{{timestamp}}",
        "location": {
          "name": "{{city}}",
          "coordinates": {
            "lat": "{{float(-90.0,90.0)}}",
            "lng": "{{float(-180.0,180.0)}}"
          }
        },
        "device": "{{randomOf(mobile,desktop,tablet)}}",
        "platform": "{{randomOf(ios,android,web)}}"
      }
    }
  },
  "containerTemplates": {
    "optionalUser": {
      "id": "{{int(1,1000)}}",
      "name": "{{optional(name)}}",
      "email": "{{optional(email)}}",
      "phone": "{{optional(phone)}}"
    },
    "futureData": {
      "id": "{{uuid}}",
      "asyncResult": "{{future(string)}}",
      "computation": "{{callable(int)}}",
      "supplier": "{{supplier(boolean)}}"
    },
    "reactiveData": {
      "id": "{{uuid}}",
      "monoValue": "{{mono(string)}}",
      "fluxValues": "{{flux(int,5)}}",
      "singleValue": "{{single(float)}}"
    }
  },
  "boundaryTemplates": {
    "extremeValues": {
      "maxInt": "{{int(2147483647,2147483647)}}",
      "minInt": "{{int(-2147483648,-2147483648)}}",
      "maxLong": "{{long(9223372036854775807,9223372036854775807)}}",
      "minLong": "{{long(-9223372036854775808,-9223372036854775808)}}",
      "maxFloat": "{{float(3.4028235E38,3.4028235E38)}}",
      "minFloat": "{{float(1.4E-45,1.4E-45)}}",
      "emptyString": "",
      "longString": "{{string(10000)}}",
      "emptyArray": [],
      "largeArray": "{{array(int,1000)}}"
    },
    "nullValues": {
      "nullString": null,
      "nullNumber": null,
      "nullObject": null,
      "nullArray": null
    },
    "specialCharacters": {
      "unicode": "{{unicode}}",
      "emoji": "{{emoji}}",
      "specialChars": "{{specialChars}}",
      "htmlEntities": "{{htmlEntities}}",
      "jsonEscape": "{{jsonEscape}}"
    }
  },
  "performanceTemplates": {
    "largeObject": {
      "id": "{{uuid}}",
      "data": "{{array(object,1000)}}",
      "metadata": {
        "fields": "{{array(string,100)}}",
        "values": "{{array(float,100)}}",
        "timestamps": "{{array(timestamp,100)}}"
      }
    },
    "deepNesting": {
      "level1": {
        "level2": {
          "level3": {
            "level4": {
              "level5": {
                "level6": {
                  "level7": {
                    "level8": {
                      "level9": {
                        "level10": {
                          "data": "{{string}}",
                          "value": "{{int}}"
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  },
  "errorTemplates": {
    "invalidPlaceholder": {
      "id": "{{invalidPlaceholder}}",
      "name": "{{unknownFunction()}}"
    },
    "malformedJson": "{ \"id\": {{int}}, \"name\": }",
    "circularReference": {
      "id": "{{uuid}}",
      "parent": "{{self}}"
    }
  },
  "customTemplates": {
    "customUser": {
      "id": "{{custom.userId}}",
      "name": "{{custom.fullName}}",
      "role": "{{custom.userRole}}",
      "permissions": "{{custom.permissions}}"
    },
    "businessEntity": {
      "companyId": "{{custom.companyId}}",
      "department": "{{custom.department}}",
      "budget": "{{custom.budget}}",
      "fiscal": {
        "year": "{{custom.fiscalYear}}",
        "quarter": "{{custom.quarter}}"
      }
    }
  }
}